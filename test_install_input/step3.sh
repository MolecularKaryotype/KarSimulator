# please copy the code and run in the /KarSimulator directory
# generates 5 KT files, each with 5 random SVs (not allowing chromosomal deletion/duplication),
# on top of the file from last step, which already contains 2 aneuploidies
python KarSimulator.py random --json test_install_input/step3_parameter.JSON
